{
  "name": "multimodal-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Multimodal RAG Academic Memory Assistant",
  "scripts": {
    "install:backend": "pip install -r backend/requirements.txt",
    "install:frontend": "pnpm install --prefix frontend",
    "install:all": "npm run install:backend && npm run install:frontend",
    "dev:backend": "set PYTHONIOENCODING=utf-8 && backend\\venv\\Scripts\\python.exe -m uvicorn app.main:app --reload --app-dir backend --host 0.0.0.0 --port 8000",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "npm run build --prefix frontend",
    "start:frontend": "npm run start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
